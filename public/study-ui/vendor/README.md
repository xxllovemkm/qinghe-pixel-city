# Browser dependencies

Local browser builds from the npm registry. Versions, upstream tarballs, integrity values and file hashes are recorded in `manifest.json`. Each package includes its upstream license.

- markdown-it: Markdown parsing.
- KaTeX: mathematical typesetting and local fonts.
- DOMPurify: HTML, SVG and MathML sanitization.
